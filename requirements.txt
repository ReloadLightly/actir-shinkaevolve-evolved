# Stage A (API-free foundation) needs only these two.
pyyaml>=6.0
pytest>=8.0

# Stage B onward: the frozen judge's Anthropic backend.
# anthropic>=0.116.0

# The evolution engine itself (Apache-2.0). Not required for Stage A: evaluate.py
# falls back to an equivalent local harness when it is absent.
#
#   pip install git+https://github.com/SakanaAI/ShinkaEvolve.git
#
# VERIFIED 2026-08-17: installed from that URL, and all nine run configs
# construct EvolutionConfig/DatabaseConfig/LocalJobConfig cleanly against it.
# The engine integration is no longer an unknown.
#
# DO NOT `pip install shinka`. That name on PyPI belongs to an unrelated
# IMAGE-UPSCALING package by a different author. It imports cleanly and even
# provides a `shinka.core`, so it looks installed and is not — the harness would
# silently fall back and you would believe the real engine had run. There is a
# guard for exactly this: `_eval_harness.shinka_is_real()`, which keys on the
# entry point rather than on the module existing.
